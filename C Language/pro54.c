#include<stdio.h>

/**
    initial values

    note : if you don't assign any value then
    variable have a garbage values
*/

void main(){
    int ip1, ip2, ip3;

    printf("\n value of ip1 is %d", ip1);
    printf("\n value of ip2 is %d", ip2);
    printf("\n value of ip3 is %d", ip3);
}
